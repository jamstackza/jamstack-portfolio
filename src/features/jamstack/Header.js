import React, { Component } from 'react';

export default class Header extends Component {
  static propTypes = {

  };

  render() {
    return (
      <div className="jamstack-header">
        Component content: jamstack/Header
      </div>
    );
  }
}
