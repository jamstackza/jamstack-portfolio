import React, { Component } from 'react';

export default class SideSwipe extends Component {
  static propTypes = {

  };

  render() {
    return (
      <div className="common-side-swipe">
        Component content: common/SideSwipe
      </div>
    );
  }
}
